# ========================================
# MONGODB & REDIS SCREENSHOT COMMANDS
# ========================================

# STEP 1: Always go to project directory first!
cd /Users/sujithdugyala/Desktop/DSGP\ 2

# ========================================
# MONGODB SCREENSHOTS
# ========================================

# MongoDB Screenshot 1: List all databases
docker-compose exec mongodb mongosh --eval "db.adminCommand('listDatabases')"

# MongoDB Screenshot 2: Show collections in kayak_db
docker-compose exec mongodb mongosh kayak_db --eval "db.getCollectionNames()"

# MongoDB Screenshot 3: Count documents in each collection
docker-compose exec mongodb mongosh kayak_db --eval "db.getCollectionNames().forEach(function(col) { print(col + ': ' + db[col].countDocuments()); })"

# MongoDB Screenshot 4: Show a sample review document
docker-compose exec mongodb mongosh kayak_db --eval "db.reviews.findOne()"

# MongoDB Screenshot 5: Show email logs (Kafka integration proof)
docker-compose exec mongodb mongosh kayak_db --eval "db.email_logs.find().limit(3).toArray()"

# ========================================
# REDIS SCREENSHOTS
# ========================================

# Redis Screenshot 1: Show number of cached keys
docker-compose exec redis redis-cli DBSIZE

# Redis Screenshot 2: Show cache keys (first 20)
docker-compose exec redis redis-cli KEYS '*' | head -20

# Redis Screenshot 3: Show cache hit/miss stats (PERFORMANCE!)
docker-compose exec redis redis-cli INFO stats | grep -E "keyspace_hits|keyspace_misses"

# Redis Screenshot 4: Show memory usage
docker-compose exec redis redis-cli INFO memory | grep -E "used_memory_human|maxmemory"

# Redis Screenshot 5: Show a sample cached value (if exists)
docker-compose exec redis redis-cli GET "flight:search:SFO:LAX"

# ========================================
# BONUS: Show Kafka Topics
# ========================================

# Kafka Screenshot: List all topics
docker-compose exec kafka kafka-topics --list --bootstrap-server localhost:9092

# ========================================
# DONE! You should have:
# - 5 MongoDB screenshots
# - 5 Redis screenshots  
# - 1 Kafka screenshot
# ========================================
